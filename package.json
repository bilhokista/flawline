{
  "name": "thesis-os-workspace",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w thesis-os",
    "test": "npm run test -w thesis-os",
    "lint": "npm run lint -w thesis-os"
  }
}
