{
  "name": "flawline-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w flawline",
    "test": "npm run test -w flawline",
    "lint": "npm run lint -w flawline"
  }
}
